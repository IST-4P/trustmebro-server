services:
  kafka:
    container_name: kafka_server
    image: bitnamilegacy/kafka
    environment:
      - KAFKA_CFG_NODE_ID=0
      - "KAFKA_CFG_PROCESS_ROLES=controller,broker"
      - "KAFKA_CFG_LISTENERS=PLAINTEXT://0.0.0.0:9092,CONTROLLER://0.0.0.0:9093,EXTERNAL://0.0.0.0:29092"
      - "KAFKA_CFG_LISTENER_SECURITY_PROTOCOL_MAP=PLAINTEXT:PLAINTEXT,CONTROLLER:PLAINTEXT,EXTERNAL:PLAINTEXT"
      - "KAFKA_CFG_CONTROLLER_QUORUM_VOTERS=0@localhost:9093"
      - KAFKA_CFG_CONTROLLER_LISTENER_NAMES=CONTROLLER
      - "KAFKA_CFG_ADVERTISED_LISTENERS=PLAINTEXT://kafka:9092,EXTERNAL://localhost:29092"
      - KAFKA_CFG_AUTO_CREATE_TOPICS_ENABLE=true
      - ALLOW_PLAINTEXT_LISTENER=yes
    volumes:
      - kafka_data:/bitnami/kafka
    networks:
      - trustmebro-network
    restart: unless-stopped
    healthcheck:
      test:
        - CMD-SHELL
        - "kafka-broker-api-versions.sh --bootstrap-server localhost:9092 || exit 1"
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 30s

  redis:
    image: redis:7
    container_name: redis_server
    restart: unless-stopped
    command: ["redis-server", "--requirepass", "${REDIS_PASSWORD}"]
    environment:
      - REDIS_PASSWORD=${REDIS_PASSWORD}
    networks:
      - trustmebro-network
    volumes:
      - redis_data:/data
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 5s
      retries: 5

  loki:
    image: grafana/loki:3.0.0
    container_name: loki
    command: -config.file=/etc/loki/local-config.yaml
    networks:
      - trustmebro-network
    healthcheck:
      test: ["CMD", "wget", "-qO-", "http://localhost:3100/ready"]
      interval: 60s
      timeout: 5s
      retries: 3
      start_period: 20s

  promtail:
    image: grafana/promtail:3.0.0
    container_name: promtail
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
      - ./docker/promtail-config.yaml:/etc/promtail/config.yaml
    command: -config.file=/etc/promtail/config.yaml
    networks:
      - trustmebro-network
    depends_on:
      - loki
    healthcheck:
      test:
        [
          "CMD-SHELL",
          "test -e /proc/1/cmdline && grep -q promtail /proc/1/cmdline",
        ]
      interval: 60s
      timeout: 5s
      retries: 3
      start_period: 10s

  grafana:
    image: grafana/grafana:10.2.0
    container_name: grafana
    ports:
      - "3500:3000"
    environment:
      - GF_SECURITY_ADMIN_USER=${GRAFANA_ADMIN}
      - GF_SECURITY_ADMIN_PASSWORD=${GRAFANA_ADMIN_PASSWORD}
      - GF_USERS_ALLOW_SIGN_UP=false
    networks:
      - trustmebro-network
    volumes:
      - grafana_data:/var/lib/grafana
    depends_on:
      - loki
    healthcheck:
      test: ["CMD", "wget", "-qO-", "http://localhost:3000/api/health"]
      interval: 30s
      timeout: 5s
      retries: 5
      start_period: 30s

  tusd:
    image: tusproject/tusd:latest
    container_name: tusd_server
    depends_on:
      - minio
    ports:
      - "1080:1080"
    environment:
      AWS_ACCESS_KEY_ID: ${MINIO_ACCESS_KEY}
      AWS_SECRET_ACCESS_KEY: ${MINIO_SECRET_KEY}
      AWS_REGION: ${MINIO_REGION}
    networks:
      - trustmebro-network
    command: >
      -port=1080
      -base-path=/files/
      -s3-bucket=videos
      -s3-endpoint=http://minio:9000
      -s3-object-prefix=tus/
      -cors-allow-credentials=true
      -cors-allow-headers=Cookie
      -cors-allow-origin=https://tusd.hacmieu.xyz
      -hooks-http=http://web:3100/api/v1/media/tusd
      -hooks-enabled-events=pre-create,post-create,post-finish
    volumes:
      - tusd_data:/data
    healthcheck:
      test: ["CMD", "wget", "-qO-", "http://localhost:1080/health"]
      interval: 60s
      timeout: 10s
      retries: 3

  minio:
    image: "ghcr.io/coollabsio/minio:RELEASE.2025-10-15T17-29-55Z"
    command: 'server /data --console-address ":9001"'
    ports:
      - "9000:9000"
      - "9001:9001"
    environment:
      - MINIO_SERVER_URL=${MINIO_SERVER_URL}
      - MINIO_BROWSER_REDIRECT_URL=${MINIO_BROWSER_REDIRECT_URL}
      - MINIO_ROOT_USER=${MINIO_ACCESS_KEY}
      - MINIO_ROOT_PASSWORD=${MINIO_SECRET_KEY}
    networks:
      - trustmebro-network
    volumes:
      - minio_data:/data
    healthcheck:
      test:
        - CMD
        - mc
        - ready
        - local
      interval: 5s
      timeout: 20s
      retries: 10

  user-access:
    image: caophi562005/trustmebro-user-access
    pull_policy: always
    mem_limit: 256m
    memswap_limit: 512m
    env_file:
      - .env
    ports:
      - "3000:3000"
    depends_on:
      kafka:
        condition: service_healthy
    restart: unless-stopped
    networks:
      - trustmebro-network
    healthcheck:
      test:
        [
          "CMD-SHELL",
          'node -e "fetch(''http://localhost:3000/api/v1/health/readiness'').then(r=>process.exit(r.ok?0:1)).catch(()=>process.exit(1))"',
        ]
      interval: 60s
      timeout: 20s
      retries: 3
      start_period: 60s

  media:
    image: caophi562005/trustmebro-media
    pull_policy: always
    mem_limit: 256m
    memswap_limit: 512m
    env_file:
      - .env
    ports:
      - "3001:3001"
    depends_on:
      kafka:
        condition: service_healthy
    restart: unless-stopped
    networks:
      - trustmebro-network
    healthcheck:
      test:
        [
          "CMD-SHELL",
          'node -e "fetch(''http://localhost:3001/api/v1/health/readiness'').then(r=>process.exit(r.ok?0:1)).catch(()=>process.exit(1))"',
        ]
      interval: 60s
      timeout: 20s
      retries: 3
      start_period: 60s

  role:
    image: caophi562005/trustmebro-role
    pull_policy: always
    mem_limit: 256m
    memswap_limit: 512m
    env_file:
      - .env
    ports:
      - "3002:3002"
    depends_on:
      kafka:
        condition: service_healthy
    restart: unless-stopped
    networks:
      - trustmebro-network
    healthcheck:
      test:
        [
          "CMD-SHELL",
          'node -e "fetch(''http://localhost:3002/api/v1/health/readiness'').then(r=>process.exit(r.ok?0:1)).catch(()=>process.exit(1))"',
        ]
      interval: 60s
      timeout: 20s
      retries: 3
      start_period: 60s

  product:
    image: caophi562005/trustmebro-product
    pull_policy: always
    mem_limit: 256m
    memswap_limit: 512m
    env_file:
      - .env
    ports:
      - "3003:3003"
    depends_on:
      kafka:
        condition: service_healthy
    restart: unless-stopped
    networks:
      - trustmebro-network
    healthcheck:
      test:
        [
          "CMD-SHELL",
          'node -e "fetch(''http://localhost:3003/api/v1/health/readiness'').then(r=>process.exit(r.ok?0:1)).catch(()=>process.exit(1))"',
        ]
      interval: 60s
      timeout: 20s
      retries: 3
      start_period: 60s

  query:
    image: caophi562005/trustmebro-query
    pull_policy: always
    mem_limit: 256m
    memswap_limit: 512m
    env_file:
      - .env
    ports:
      - "3004:3004"
    depends_on:
      kafka:
        condition: service_healthy
    restart: unless-stopped
    networks:
      - trustmebro-network
    healthcheck:
      test:
        [
          "CMD-SHELL",
          'node -e "fetch(''http://localhost:3004/api/v1/health/readiness'').then(r=>process.exit(r.ok?0:1)).catch(()=>process.exit(1))"',
        ]
      interval: 60s
      timeout: 20s
      retries: 3
      start_period: 60s

  notification:
    image: caophi562005/trustmebro-notification
    pull_policy: always
    mem_limit: 256m
    memswap_limit: 512m
    env_file:
      - .env
    ports:
      - "3005:3005"
    depends_on:
      kafka:
        condition: service_healthy
    restart: unless-stopped
    networks:
      - trustmebro-network
    healthcheck:
      test:
        [
          "CMD-SHELL",
          'node -e "fetch(''http://localhost:3005/api/v1/health/readiness'').then(r=>process.exit(r.ok?0:1)).catch(()=>process.exit(1))"',
        ]
      interval: 60s
      timeout: 20s
      retries: 3
      start_period: 60s

  chat:
    image: caophi562005/trustmebro-chat
    pull_policy: always
    mem_limit: 256m
    memswap_limit: 512m
    env_file:
      - .env
    ports:
      - "3006:3006"
    depends_on:
      kafka:
        condition: service_healthy
    restart: unless-stopped
    networks:
      - trustmebro-network
    healthcheck:
      test:
        [
          "CMD-SHELL",
          'node -e "fetch(''http://localhost:3006/api/v1/health/readiness'').then(r=>process.exit(r.ok?0:1)).catch(()=>process.exit(1))"',
        ]
      interval: 60s
      timeout: 20s
      retries: 3
      start_period: 60s

  cart:
    image: caophi562005/trustmebro-cart
    pull_policy: always
    mem_limit: 256m
    memswap_limit: 512m
    env_file:
      - .env
    ports:
      - "3007:3007"
    depends_on:
      kafka:
        condition: service_healthy
    restart: unless-stopped
    networks:
      - trustmebro-network
    healthcheck:
      test:
        [
          "CMD-SHELL",
          'node -e "fetch(''http://localhost:3007/api/v1/health/readiness'').then(r=>process.exit(r.ok?0:1)).catch(()=>process.exit(1))"',
        ]
      interval: 60s
      timeout: 20s
      retries: 3
      start_period: 60s

  order:
    image: caophi562005/trustmebro-order
    pull_policy: always
    mem_limit: 256m
    memswap_limit: 512m
    env_file:
      - .env
    ports:
      - "3008:3008"
    depends_on:
      kafka:
        condition: service_healthy
    restart: unless-stopped
    networks:
      - trustmebro-network
    healthcheck:
      test:
        [
          "CMD-SHELL",
          'node -e "fetch(''http://localhost:3008/api/v1/health/readiness'').then(r=>process.exit(r.ok?0:1)).catch(()=>process.exit(1))"',
        ]
      interval: 60s
      timeout: 20s
      retries: 3
      start_period: 60s

  payment:
    image: caophi562005/trustmebro-payment
    pull_policy: always
    mem_limit: 256m
    memswap_limit: 512m
    env_file:
      - .env
    ports:
      - "3009:3009"
    depends_on:
      kafka:
        condition: service_healthy
    restart: unless-stopped
    networks:
      - trustmebro-network
    healthcheck:
      test:
        [
          "CMD-SHELL",
          'node -e "fetch(''http://localhost:3009/api/v1/health/readiness'').then(r=>process.exit(r.ok?0:1)).catch(()=>process.exit(1))"',
        ]
      interval: 60s
      timeout: 20s
      retries: 3
      start_period: 60s

  web:
    image: caophi562005/trustmebro-web
    pull_policy: always
    mem_limit: 256m
    memswap_limit: 512m
    env_file:
      - .env
    ports:
      - "3100:3100"
    depends_on:
      kafka:
        condition: service_healthy
      role: # Thêm dependency này
        condition: service_started
    restart: unless-stopped
    networks:
      - trustmebro-network
    healthcheck:
      test:
        [
          "CMD-SHELL",
          'node -e "fetch(''http://localhost:3100/api/v1/health/readiness'').then(r=>process.exit(r.ok?0:1)).catch(()=>process.exit(1))"',
        ]
      interval: 60s
      timeout: 20s
      retries: 3
      start_period: 60s

  admin:
    image: caophi562005/trustmebro-admin
    pull_policy: always
    mem_limit: 256m
    memswap_limit: 512m
    env_file:
      - .env
    ports:
      - "3200:3200"
    depends_on:
      kafka:
        condition: service_healthy
      role: # Thêm dependency này
        condition: service_started
    restart: unless-stopped
    networks:
      - trustmebro-network
    healthcheck:
      test:
        [
          "CMD-SHELL",
          'node -e "fetch(''http://localhost:3200/api/v1/health/readiness'').then(r=>process.exit(r.ok?0:1)).catch(()=>process.exit(1))"',
        ]
      interval: 60s
      timeout: 20s
      retries: 3
      start_period: 60s

networks:
  trustmebro-network:
    driver: bridge
    name: trustmebro-network
volumes:
  kafka_data:
    driver: local
  redis_data:
    driver: local
  grafana_data:
    driver: local
  tusd_data:
    driver: local
  minio_data:
    driver: local
