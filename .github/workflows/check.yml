name: CI/CD - Detect Affected Apps

on:
  push:
    branches:
      - sauvole565
  pull_request:
    branches:
      - sauvole565

permissions:
  actions: read
  contents: read

jobs:
  detect-affected:
    runs-on: ubuntu-latest
    outputs:
      affected-apps: ${{ steps.affected.outputs.apps }}
    steps:
      # ⭐ STEP 1: Checkout with full history
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0 # QUAN TRỌNG: Full git history to detect affected

      # ⭐ STEP 2: Setup pnpm
      - uses: pnpm/action-setup@v4
        with:
          version: 9

      # ⭐ STEP 3: Setup Node
      - uses: actions/setup-node@v4
        with:
          node-version: 22

      # ⭐ STEP 4: Configure Git
      - name: Configure Git protocol
        run: git config --global url."https://github.com/".insteadOf git://github.com/

      # ⭐ STEP 5: Cache pnpm
      - name: Cache pnpm store
        id: cache-pnpm-store
        uses: actions/cache@v4
        with:
          path: |
            ~/.pnpm-store
            node_modules
          key: ${{ runner.os }}-pnpm-${{ hashFiles('**/pnpm-lock.yaml') }}
          restore-keys: |
            ${{ runner.os }}-pnpm-

      # ⭐ STEP 6: Install dependencies
      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      # ⭐ STEP 7: DETECT AFFECTED APPS
      - name: Detect affected apps
        id: affected
        run: |
          # Try Nx first (if using Nx)
          AFFECTED=$(npx nx print-affected --base=origin/sauvole565 -t build --select=projects --plain 2>/dev/null || echo "")

          # Fallback to git diff
          if [ -z "$AFFECTED" ]; then
            # Get changed files in apps/services and apps/bffs
            APPS=$(git diff --name-only origin/sauvole565...HEAD | grep -E '^apps/(services|bffs)/' | awk -F'/' '{print $3}' | sort -u)
            AFFECTED=$(echo "$APPS" | paste -sd, -)
          fi

          echo "apps=$AFFECTED" >> $GITHUB_OUTPUT

      # ⭐ STEP 8: LOG AFFECTED APPS (MAIN PURPOSE)
      - name: Log affected apps
        run: |
          AFFECTED="${{ steps.affected.outputs.apps }}"

          echo ""
          echo "=========================================="
          echo "📦 AFFECTED APPS DETECTION"
          echo "=========================================="
          echo "Branch: sauvole565"
          echo "Comparing: origin/sauvole565 vs HEAD"
          echo ""

          if [ -z "$AFFECTED" ]; then
            echo "❌ No apps affected"
            echo "   Only config/docs/CI files changed"
          else
            TOTAL=$(echo "$AFFECTED" | tr ',' '\n' | wc -l)
            echo "✅ $TOTAL app(s) affected:"
            echo ""
            echo "$AFFECTED" | tr ',' '\n' | while read -r app; do
              if [ ! -z "$app" ]; then
                echo "   📱 $app"
              fi
            done
          fi

          echo ""
          echo "=========================================="

      # ⭐ STEP 9: Detailed summary
      - name: Detailed affected apps summary
        run: |
          AFFECTED="${{ steps.affected.outputs.apps }}"

          if [ -z "$AFFECTED" ]; then
            echo "⏭️  Skipping: No apps to build"
          else
            echo ""
            echo "📊 DETAILED REPORT"
            echo ""
            echo "Affected Apps:"
            echo "$AFFECTED" | tr ',' '\n' | nl
            echo ""
            echo "Count: $(echo "$AFFECTED" | tr ',' '\n' | wc -l)"
          fi

      # ⭐ STEP 10: Show git diff for reference
      - name: Show changed files (for reference)
        run: |
          echo ""
          echo "Changed files in this push:"
          echo "================================================"
          git diff --name-only origin/sauvole565...HEAD | head -20

          TOTAL_CHANGED=$(git diff --name-only origin/sauvole565...HEAD | wc -l)
          if [ $TOTAL_CHANGED -gt 20 ]; then
            echo "... and $((TOTAL_CHANGED - 20)) more files"
          fi
          echo "================================================"
          echo ""
